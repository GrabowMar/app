# PostgreSQL
# `just bootstrap` generates random USER / PASSWORD automatically.
# ------------------------------------------------------------------------------
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=llm_lab
POSTGRES_USER=changeme
POSTGRES_PASSWORD=changeme
